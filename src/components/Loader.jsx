import React from "react";

import "@asset/component/Loader.scss";

const Loader = () => {
  return (
    <section className="loader-page">
      <div className="loader"></div>
    </section>
  );
};

export default Loader;
